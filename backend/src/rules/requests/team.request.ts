export interface CreateSchedulePresent {
  teamId: string;
  trialDate: string;
  officialDate: string[];
}
export interface SubmissionType {
  slideLink: string;
  taskAssignmentLink: string;
  productLinks: string[];
  note: string;
}
