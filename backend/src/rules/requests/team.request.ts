export interface CreateSchedulePresent {
    teamId: string;
    trialDate: string;
    officialDate: string[];
}
export interface SubmissionType {
    teamId: string;
    presentationLink: string;
    productLink: string;
    note: string;
}
